[package]
name = "server-cat-agent"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Server Cat local monitoring agent"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
